# Copy to .env.local (gitignored) and adjust.
# Canonical URL of the deployed site, used for metadataBase, sitemap, robots.
# Falls back to https://example.com when unset or empty, so CI never needs it.
NEXT_PUBLIC_SITE_URL=https://example.com
