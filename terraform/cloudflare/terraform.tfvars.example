# Copy to terraform.tfvars (gitignored by convention; keep it secret-free anyway).
cloudflare_account_id = "your-cloudflare-account-id"
project_name          = "my-site"
domain                = "example.com"
zone_name             = "example.com"
